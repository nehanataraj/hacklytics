# Copy this file to .env.local and fill in your values. Never commit .env.local.

# Databricks (optional — for NPC Studio Data page and sync)
# Fill in HOST, ACCESS_TOKEN, WAREHOUSE_ID; table and query are ready for auto-sync + graphs.
# DATABRICKS_HOST=https://your-workspace.cloud.databricks.com
# DATABRICKS_ACCESS_TOKEN=dapi...
# DATABRICKS_WAREHOUSE_ID=your-warehouse-id
# App creates this table on first sync; use catalog.schema.table (e.g. main.default.npcs)
DATABRICKS_SYNC_TABLE=main.default.npcs
# Data page graphs use this query — keep it the same table as above
DATABRICKS_NPC_QUERY=SELECT * FROM main.default.npcs

# Langfuse (optional — for NPC chat observability)
# LANGFUSE_SECRET_KEY=
# LANGFUSE_PUBLIC_KEY=
# LANGFUSE_BASE_URL=https://cloud.langfuse.com

# Gemini (for NPC chat)
# GEMINI_API_KEY=
